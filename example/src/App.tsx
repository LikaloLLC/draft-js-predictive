import React from 'react';

import { ExampleComponent } from 'predictable';

import 'predictable/dist/index.css';

export default () => {
  return (
    <ExampleComponent text="Predictable plugin for draft-js" />
  );
}
